# AR Services - Экран зависимостей

Реализация экрана управления зависимостями между микросервисами для приложения AR Services.

## 🎯 Реализованный функционал

### ✅ Основные возможности

1. **Отображение списка зависимостей**
   - Группировка зависимостей по сервисам
   - Табличное представление с детальной информацией
   - Отображение типа зависимости, статуса и описания

2. **Добавление зависимостей**
   - Модальное окно для создания новой зависимости
   - Выбор сервиса и зависимого сервиса из списка
   - Выбор типа зависимости (Синхронная, Асинхронная, База данных)
   - Добавление описания зависимости

3. **Удаление зависимостей**
   - Кнопка удаления для каждой зависимости
   - Подтверждение удаления

4. **Поиск и фильтрация**
   - Поиск по названию сервиса или описанию
   - Фильтрация по типу зависимости
   - Отображение статистики (количество зависимостей и сервисов)

5. **Построение графа зависимостей**
   - Визуализация зависимостей в виде графа
   - Интерактивные узлы (сервисы) и рёбра (зависимости)
   - Масштабирование и перемещение графа
   - Легенда с типами зависимостей

## 📁 Структура файлов

### Модели данных
- `Models/ServiceModels.swift` - Расширенные модели с поддержкой зависимостей
  - `DependencyResponse` - Модель зависимости
  - `CreateDependencyRequest` - Запрос создания зависимости
  - `DependencyType` - Типы зависимостей
  - `DependencyGraph` - Модель графа зависимостей

### API сервисы
- `Services/APIService.swift` - Расширенный API сервис с методами для зависимостей
- `Services/MockAPIService.swift` - Mock сервис для тестирования

### ViewModels
- `ViewModels/DependencyViewModel.swift` - ViewModel для управления зависимостями

### Views
- `Views/DependenciesView.swift` - Основной экран зависимостей
- `Views/CreateDependencyView.swift` - Модальное окно создания зависимости
- `Views/DependencyGraphView.swift` - Визуализация графа зависимостей

## 🔧 Типы зависимостей

### Синхронные зависимости
- **Описание**: Прямые HTTP вызовы между сервисами
- **Использование**: REST API вызовы, синхронная обработка данных
- **Цвет в интерфейсе**: Синий

### Асинхронные зависимости
- **Описание**: Взаимодействие через очереди сообщений
- **Использование**: Event-driven архитектура, фоновая обработка
- **Цвет в интерфейсе**: Оранжевый

### База данных
- **Описание**: Общие базы данных между сервисами
- **Использование**: Shared database pattern, общие справочники
- **Цвет в интерфейсе**: Фиолетовый

## 🎨 Интерфейс

### Основной экран
- Заголовок с кнопками "Граф зависимостей" и "Добавить зависимость"
- Поле поиска и фильтр по типу зависимости
- Статистика (количество зависимостей и сервисов)
- Список зависимостей, сгруппированных по сервисам
- Информационная секция с описанием типов зависимостей

### Модальное окно добавления
- Выбор основного сервиса
- Выбор зависимого сервиса
- Выбор типа зависимости
- Поле для описания
- Кнопки "Отмена" и "Добавить"

### Граф зависимостей
- Интерактивная визуализация
- Узлы представляют сервисы (с цветовой кодировкой по типу)
- Рёбра представляют зависимости (с цветовой кодировкой по типу)
- Возможность масштабирования и перемещения
- Информация о выбранном узле
- Легенда с типами зависимостей

## 🧪 Тестирование

Для тестирования функционала используется `MockAPIService` с предустановленными данными:

### Тестовые сервисы
- `user-service` - Сервис управления пользователями
- `auth-service` - Сервис аутентификации
- `order-service` - Сервис обработки заказов
- `payment-service` - Сервис обработки платежей

### Тестовые зависимости
1. `user-service` → `auth-service` (Синхронная)
2. `order-service` → `user-service` (Асинхронная)
3. `order-service` → `payment-service` (Синхронная)
4. `payment-service` → `user-service` (База данных)

## 🌐 Демо

Создан HTML прототип интерфейса для демонстрации функционала:
- `dependencies-demo.html` - Интерактивная демонстрация интерфейса

## 🚀 API Endpoints

### Зависимости
- `GET /api/v1/dependencies` - Получение всех зависимостей
- `GET /api/v1/services/{id}/dependencies` - Получение зависимостей сервиса
- `POST /api/v1/dependencies` - Создание зависимости
- `PATCH /api/v1/dependencies/{id}` - Обновление зависимости
- `DELETE /api/v1/dependencies/{id}` - Удаление зависимости
- `GET /api/v1/dependencies/graph` - Получение графа зависимостей

## 📱 Интеграция

Экран зависимостей интегрирован в основное приложение через:
- Обновление `ARServicesMainView.swift` - добавлена вкладка "Зависимости"
- Использование существующих компонентов из `DashboardComponents.swift`
- Совместимость с существующей архитектурой приложения

## 🔄 Состояния интерфейса

### Загрузка
- Индикатор загрузки при получении данных
- Скелетон-экраны для плавного UX

### Пустое состояние
- Информативное сообщение при отсутствии зависимостей
- Кнопка для добавления первой зависимости

### Ошибки
- Обработка сетевых ошибок
- Информативные сообщения об ошибках
- Возможность повторной попытки

### Поиск без результатов
- Сообщение о том, что ничего не найдено
- Предложение изменить критерии поиска

## 🎯 Следующие шаги

1. **Интеграция с реальным API**
   - Замена MockAPIService на реальные API вызовы
   - Обработка реальных ошибок сети

2. **Расширенная визуализация графа**
   - Более сложные алгоритмы размещения узлов
   - Анимации переходов
   - Экспорт графа в различные форматы

3. **Дополнительные фильтры**
   - Фильтрация по владельцу сервиса
   - Фильтрация по дате создания
   - Сохранение настроек фильтров

4. **Аналитика зависимостей**
   - Обнаружение циклических зависимостей
   - Анализ критического пути
   - Метрики связности сервисов

## 📋 Требования

- iOS 15.0+
- macOS 12.0+
- SwiftUI 3.0+
- Swift 5.5+

## 🏗️ Архитектура

Проект следует MVVM архитектуре:
- **Models**: Модели данных и бизнес-логика
- **Views**: SwiftUI представления
- **ViewModels**: Связующий слой между View и Model
- **Services**: Сетевые сервисы и API клиенты